{
  "name": "dharmic-data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "test:unit": "node --test tests/vlog.unit.test.mjs",
    "test:integration": "node --test tests/build.integration.test.mjs",
    "test:acceptance": "node scripts/verify-dharmic.mjs",
    "test": "npm run test:unit && npm run test:integration"
  },
  "dependencies": {
    "astro": "^7.0.9"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
