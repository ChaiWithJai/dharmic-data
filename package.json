{
  "name": "dharmic-data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "test": "npm run build && npm run verify:dharmic",
    "verify:dharmic": "node scripts/verify-dharmic.mjs",
    "preview": "astro preview --host 0.0.0.0"
  },
  "dependencies": {
    "astro": "^7.0.9"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
