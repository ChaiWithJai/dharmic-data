# Dharmic Data working context

This is the `ChaiWithJai/dharmic-data` repository. The surrounding `qedc` folder is a different repository with unrelated work. Resolve this checkout before Git, npm or deployment commands. Never infer the target repository from the shell's starting directory.

Use `node /absolute/path/to/this/checkout/scripts/dd.mjs status` to confirm the root and branch. The same helper accepts `build`, `test`, `dev` and `review`. It finds its own Git root and sets the command's working directory.

Read `GOAL.md` and `docs/teacher-school-website.md` before continuing this website work. The film production goal remains paused. Website work does not authorize restarting film generation, rendering, model evaluation or Buzz workers.

The working film is available only in the local review server. Do not copy it into `public`, commit it, or set a public film URL until the release status is resolved. The production site uses an original poster in the meantime.

Preserve the original visual template. Jai explicitly rejected replacing it with a new editorial design. Use the existing Webflow bundle, Fredoka/Inter type, bright sticker and pill treatments, illustrated characters and Rive animations. Spread richer content across pages. A request to change the story does not imply a request to replace the visual identity.

The school and teaching promise lead the website. The film supports the founder story. Preserve the existing shop, vlog, learner accounts and published case studies when changing the homepage.

Maven owns enrollment and the resource signup and delivery flow. Do not add an inactive email form or claim that an email was delivered without testing it. Do not send a broadcast to subscribers as part of a website update.

The primary lead magnet is now **Turn your team's files into gold**, a practical guide to shared team context using GPT-6 Astra and local AI. Read `docs/team-digital-twin-guide.md` before changing it. Preserve the same Maven resource URL. Clearly label the worked example as synthetic and distinguish integrity checks from model performance or workplace results. Do not turn the guide back into a film retrospective or treat the Codex subscription as an application API.

Check the site identity and production deploy before publishing. Keep credentials out of source, screenshots, logs and issue bodies.
